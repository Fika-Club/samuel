const App = () => {
    return (
        <div>
            <h1>Emotion Example</h1>
            <p>This is a simple example of using Emotion for styling in React.</p>
        </div>
    );
};

export default App;
